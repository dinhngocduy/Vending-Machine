import { GET_TREATMENT_HISTORY_DETAIL } from './../actions/index';
import { all, takeLatest, fork } from 'redux-saga/effects';
import { GET_SETTING_ADMIN } from 'redux/actions';
import { GET_SUPPLIER } from '../actions/index';

export default function* rootSaga() {
  yield all([
    
  ]);
}
