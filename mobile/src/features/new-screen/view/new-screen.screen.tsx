import React from 'react';
import { View } from 'react-native';
import { NewScreenAdapter } from '../../../../../vending-core/model-new-screen/new-screen.adapter';

export const NewScreenContainer = () => {
  const { demo } = NewScreenAdapter();

  // TODO code giao dien o day
  return <View></View>;
};
